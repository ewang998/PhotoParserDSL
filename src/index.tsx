import * as React from 'react';
import * as ReactDOM from 'react-dom';
import DSLForm from './components/DSLForm';

ReactDOM.render(
  <div>
    <DSLForm />
  </div>,
  document.getElementById('root') as HTMLElement
);
